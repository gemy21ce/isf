<?phpclass User extends DataMapper {    var $table = 'users';    var $model = 'user';        var $has_one = array('role','judge');    function __construct() {        parent::__construct();    }   }?>